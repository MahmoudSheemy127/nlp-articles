import {handleSubmit} from "../client/src/Input"

describe("Testing the submit functionality", () => {
            test("Testing the handleSubmit() function", () => {
           expect(handleSubmit).toBeDefined();
})});
